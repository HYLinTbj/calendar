# Binaries (root-level files only; the !/x/ lines re-include the source dirs)
/api
!/api/
/scheduler
!/scheduler/
/notification
!/notification/

# Go build cache
**/vendor/
**/.go/

# Environment files
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker volumes (local dev data)
postgres_data/
redis_data/

# Test output
*.test
*.out
coverage.html
coverage.out

# Local-only mock API helper for frontend iteration (see CLAUDE.md) — deliberately untracked
mock_api.py
